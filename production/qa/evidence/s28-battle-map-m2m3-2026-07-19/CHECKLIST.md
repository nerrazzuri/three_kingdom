# s28 · 战斗真地图化 M2（行军动画）+ M3（拖动布防）— 编辑器验证清单

日期：2026-07-19　｜　★需 Unity 编辑器验证　｜　纯 Unity 侧（不动 DLL）｜ csc 编译校验已过（0 CS 错）

## 本次改动（M2+M3，已落盘）— 兵马拆为独立可移动/可拖图元
- `ZoneBattle.uxml`：地图分三层——`zb-edges`（连线）/ `zb-regions`（地点标记，固定）/ `zb-troops`（兵马图元，可移动/可拖）。
- `ZoneBattle.uss`：新增 `.zb-troop`（绝对定位于所在区锚点，`transition: left,top 0.45s` → 换区补间"行军"）+ `.zb-troop-new`/`.zb-troop-drag`（关补间：新建就位/拖拽跟随指针）+ `.zb-troop-grip`（拖动线索）。
- `ZoneBattleController.cs`：
  - 地点标记 `BuildRegionMarker`（区名★ + 敌情反全知 + 条件徽章；我方兵马移出）。
  - `ReconcileTroops`：兵马按 detachmentId **持久复用**，摆到所在区锚点（同区下方错开）；**换区时 left/top 变→transition 沿路补间行军**；离场支队图元移除；同时刷新合法拖放落点集。
  - `AttachDrag` + `DropTargetZone`（M3）：拈起兵马→跟随指针→松手落在**相邻合法区**发 `Move` 命令（否则回弹）；姿态按钮上的按压不触发拖拽；键盘调动列表仍是无障碍兜底。

## 验证步骤
1. Unity `Assets > Refresh`，等重编译（改了 3 UI 文件）。**Console 0 编译错误**（有红错→截 `00-console.png` 记 DONE.flag）。
2. 打开 `Assets/Scenes/ZoneBattle.unity` → Play。
3. **M2 行军动画**：
   - 兵马图元（我方支队：抓手「⠿ 拖动布防」+ 将领名牌 + 兵力旗帜 + 姿态三态）**独立浮在地图上**，位于所在区锚点下方；同区多队上下错开不重叠。
   - 用**键盘调动列表**（右侧「排兵布阵」）点一个「某队 → 相邻区」——该兵马图元应**从原区平滑滑行到新区**（不是瞬移/闪现），约 0.45s。
   - 点「亲自打·推进回合」几次——若有支队在途/换位，兵马图元**沿位置补间移动**；态势刷新无报错。
4. **M3 拖动布防**：
   - 用鼠标**按住一个兵马图元拖动**——它跟随指针、微抬半透。
   - 拖到一个**相邻区**（如把预备后方的队拖到正面关城）松手——应**发出调动**，兵马补间就位到该区（等效键盘列表那次调动）。
   - 拖到**非相邻区**或空白处松手——应**回弹**到原区（不调动）。
   - 点兵马图元上的**姿态按钮**（主攻/佯攻/守）——应正常切换（不误触发拖拽）。
5. 截图 `01-troops-on-map.png`（兵马图元浮在地图上）+ `02-after-move.png`（一次调动后兵马到了新区，可选）。全程 Console 无报错。

## 判据（PASS）
- [ ] 编译 0 错误
- [ ] 兵马为**独立图元**浮在地图上（非嵌在地点卡里），同区多队错开
- [ ] 调动（键盘列表或拖动）后兵马**平滑滑行到新区**（M2 行军动画，非瞬移）
- [ ] 拖动兵马到相邻区**松手成功调动**；非相邻/空白**回弹**（M3）
- [ ] 姿态按钮点击正常、不误触拖拽；推进刷新无报错

截图存本目录；完成写 `DONE.flag`（首行 PASS 或 FAIL/问题摘要，照 s27 格式）。
备注：占位坚城底图 + USS 占位图元；真战场地图/真 sprite 后续换。兵马图元大小/错开偏移/落点半径(0.16)如需手感微调，看过后告诉我。在途兵马目前"就位到目的区并标·在途"（域内 Location=目的区），真·画在半路需 view-model 加起讫区（后续小步）。
