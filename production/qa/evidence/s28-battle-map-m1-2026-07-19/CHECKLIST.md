# s28 · 战斗真地图化 M1（地图布局骨架：锚点节点 + 邻接连线）— 编辑器验证清单

日期：2026-07-19　｜　★需 Unity 编辑器验证　｜　纯 Unity 侧改动（不动 DLL，dotnet 不受影响仍 1261）

## 本次改动（M1，已落盘）— 把"卡片方格"换成"真地图上的区域节点"
- `Assets/UI/ZoneBattle.uxml`：删除固定两行方格（zb-grid/zb-slot-*），改为**地图画布** `zb-map` + **连线层** `zb-edges`（区域节点由控制器动态生成、绝对定位）。
- `Assets/UI/ZoneBattle.uss`：战场底图从 zb-root 移到 `.zb-map`（区域节点绝对定位其上、随地图裁剪）；新增 `.zb-map`/`.zb-edges`/`.zb-node`/`.zb-node-objective`（节点 `translate:-50% -50%` 使锚点=节点中心）；移除旧 `.zb-grid/.zb-slot*`。
- `Assets/UI/ZoneBattleController.cs`：新增**数据驱动锚点表** `ZoneAnchors`（zone-id→归一化坐标）+ **邻接表** `Edges`（对齐 StandardAdjacency 6 条路）；Render 改为按锚点把区域节点绝对定位到地图上（left/top 百分比）；`DrawEdges` 用 Painter2D 在 zb-edges 层沿锚点描夯土路（节点之下）；FillZoneSlot→FillZoneNode。

## 验证步骤
1. 切 Unity 编辑器，`Assets > Refresh`，等重编译（改了 3 个 UI 文件）。
2. **Console**：0 编译错误。有红错 → 截图 `00-console.png` + 记进 DONE.flag。
3. 打开 `Assets/Scenes/ZoneBattle.unity` → Play。核对：
   - **不再是一排/两行卡片**：五个区域节点**散布在战场地图上的不同位置**（不是整齐的网格）——
     - 正面关城★ 在**中央**（金色高亮边框）；
     - 侧翼隘口 在**左侧**；
     - 遮蔽高地 在**左上**；
     - 敌粮道 在**右上（敌后）**；
     - 预备后方 在**下方（我方后方）**。
   - **邻接连线**：区域节点之间有**半透夯土色连线**（路），连出：预备—正面 · 正面—侧翼 · 正面—遮蔽 · 预备—遮蔽 · 遮蔽—粮道 · 侧翼—粮道（6 条）；连线在节点**之下**、不挡点击。
   - **节点内容照旧可读**：每个节点仍显 区名 + 我/敌兵力旗帜 + 将领名牌（剪影/立绘+阵营色边框）+ 姿态三态（主攻/佯攻/守，当前朱红高亮）+ 已成条件徽章；半透宣纸卡叠在地图上文字清晰。
   - **地图底图**：坚城背景铺满地图画布区（这是**占位底图**，非最终战场地图——真地图随后换）。
   - **推进不乱**：点「亲自打·推进回合」几次，节点态势/旗帜/名牌随回合刷新，节点仍各就各位、连线仍在，全程 Console 无报错。
   - **侧栏**：排兵布阵调动列表 + 涌现记录 照常在右侧。
4. 截图 `01-battlemap.png`（整屏——重点是"区域散布在地图上 + 连线"这个新面貌）。
5. 全程 Console 无报错。

## 判据（PASS）
- [ ] 编译 0 错误
- [ ] 五区节点**按地图位置散布**（关城中央★ / 隘口左 / 高地左上 / 粮道右上 / 预备下方），不再是网格卡片
- [ ] 邻接连线 6 条正确画出、在节点之下
- [ ] 节点内容（兵力旗帜/名牌/姿态/徽章）照旧可读
- [ ] 推进回合刷新正常、节点不错位、全程无报错

截图存本目录；完成写 `DONE.flag`（首行 PASS 或 FAIL/问题摘要，照 s27 DONE.flag 格式：逐判据 + 证据截图名 + 结论）。
备注：这是"换皮"第一期——只验**地图定位 + 连线**是否成立、读起来像不像战场地图。行军动画(M2)、拖动布防(M3)、真战场地图美术、地形切换 均后续期，本轮不验。占位坚城底图上节点坐标是否"压在合适地形"上不苛求（真地图到位后再校锚点）。
